// Placeholder Google signup function.
// Replace with real OAuth logic (likely redirect to your backend's Google auth endpoint).
export function signUpWithGoogle() {
  // Example: window.location.href = `${import.meta.env.VITE_BACKEND_URL}/api/auth/google`;
  // For now, just alert.
  alert('Google signup not yet implemented.');
}
